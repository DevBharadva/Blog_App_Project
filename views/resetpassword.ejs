<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password</title>
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background: linear-gradient(135deg, #b09aff 0%, #3781aa 100%);
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            animation: backgroundShift 3s infinite alternate ease-in-out;
        }
    
        @keyframes backgroundShift {
            0% {
                background: linear-gradient(135deg, #93cee1 0%, #3763a0 100%);
            }
            100% {
                background: linear-gradient(135deg, #a18cd1 0%, #fbc2eb 100%);
            }
        }
    
        h1 {
            color: #fff;
            font-size: 32px;
            text-align: center;
            margin-bottom: 30px;
            opacity: 0;
            animation: fadeIn 2s forwards 1s;
        }
    
        form {
            background: #333; /* Dark background for the form */
            color: #fff; /* White text color */
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
            width: 100%;
            max-width: 400px;
            opacity: 0;
            transform: translateY(30px);
            animation: slideIn 1s forwards 0.5s;
        }
    
        @keyframes fadeIn {
            to {
                opacity: 1;
            }
        }
    
        @keyframes slideIn {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    
        label {
            display: block;
            margin-bottom: 10px;
            font-size: 14px;
            color: #ccc; /* Lighter label color for contrast */
        }
    
        input{
            width: 100%;
            padding: 12px;
            border: 2px solid #555; /* Darker border for the input fields */
            border-radius: 6px;
            margin-bottom: 20px;
            font-size: 14px;
            box-sizing: border-box;
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
            background-color: #444; /* Darker background for input fields */
            color: #fff; /* White text color for input fields */
        }
        input:focus {
            border-color: #ff758c;
            box-shadow: 0 0 8px rgba(255, 117, 140, 0.5);
            outline: none;
        }
    
        button {
            background-color: #ff758c;
            color: white;
            border: none;
            margin: 10px;
            padding: 14px 28px;
            border-radius: 6px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s ease, transform 0.2s ease;
            width: 100%;
        }
    
        button:hover {
            background-color: #ff6175;
            transform: translateY(-2px);
        }
    
        button:active {
            background-color: #ff495e;
            transform: translateY(0);
        }
    </style>
</head>
<body>
    <form action="/api/user/resetpassword" method="post">
            <h2>Reset Password</h2>
                <label for="newpassword">New Password</label>
                <input type="password" id="newpassword" name="newpassword" required>
                <label for="confirm-password">Confirm Password</label>
                <input type="password" id="confirmpassword" name="confirmpassword" required>

            <button type="submit" class="btn">Reset Password</button>
        </form>
    </div>
</body>
</html>
